# Runtime dependencies for Streamlit Community Cloud deployment.
# NOTE: Select Python 3.12 in the Streamlit Cloud "Advanced settings" — the
# project supports 3.10+, but Community Cloud does not yet offer 3.14.
#
# torch (the optional LSTM model) is intentionally omitted: it is too large for
# the ~1 GB Community Cloud resource limit. The pipeline degrades gracefully and
# runs LightGBM + SARIMA without it. To enable the LSTM locally: pip install torch
pandas>=2.0
polars>=0.20
pyarrow>=14.0
duckdb>=0.10
numpy>=1.24
pyyaml>=6.0
pydantic>=2.0
pydantic-settings>=2.0
requests>=2.31
tenacity>=8.2
statsmodels>=0.14
scikit-learn>=1.3
lightgbm>=4.0
rich>=13.0
typer>=0.9
beautifulsoup4>=4.12
lxml>=5.0
xlrd>=2.0
openpyxl>=3.1
streamlit>=1.30
plotly>=5.18
fastapi>=0.110
uvicorn>=0.27

# Install the local package itself (provides the `nhs_forecast` module).
# NOTE: a regular (non-editable) install — Streamlit Community Cloud's build
# sandbox cannot create editable (`-e .`) links and fails with
# "Error installing requirements".
.
